# Frontend environment (client/.env)
# Leave empty to call the API on the same origin the client is served from.
# In development that means the Vite server's /api proxy to port 5001.
# For separated deployments, set this to the public backend origin, e.g.:
#   VITE_API_URL=https://api.example.com
VITE_API_URL=